# Testcontainers configuration for Windows/Docker Desktop
# This helps Testcontainers properly detect and communicate with Docker

# Enable reuse of containers across test runs for better performance
testcontainers.reuse.enable=true

# Check Docker availability
checks.disable=false

# Increase Testcontainers startup timeout
testcontainers.startup.timeout=120
